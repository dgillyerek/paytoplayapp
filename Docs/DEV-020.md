# DEV-020 — Thin teach T1–T3

Status: **wired in Play Mode** after DEV-018 Front Garden splash. **Not** full FTUE 0–12.

Once per save until **Order 1** completes (`ITeachSave` / PlayerPrefs `grove.teach.order1.done`).

| Beat | When | Highlight | Copy | Advance |
| --- | --- | --- | --- | --- |
| T1 Crate | After Front Garden splash | Crate + teach ring | Tap the crate to grow supplies. | Crate tap ≥1 |
| T2 Merge | After first spit / 2× matching | Two matching cells (world rings) | Drag two matches together. | One stack or merge; magnet/ghost if idle 4s |
| T3 Deliver | Order 1 WF T2 ready | Active order card + item | Deliver to Maya. | Complete Order 1 |

Skip (“Got it”) is offered **after T1** only if the player already acted correctly (already stacked/merged, or Order 1 already ready).

OUT: inventory / dig / herb lessons, FTUE 0–12.

DES-004 teach-ring PNGs in `design/unity-drop` map to stub `UI_TeachRing` (fallback: `ENV_FG_CellHighlight`).
