# DEV-020 — Thin teach T1–T3

Status: **wired in Play Mode** after DEV-018 Front Garden splash. **Not** full FTUE 0–12.

Once per save until **Order 1** completes (`ITeachSave` / PlayerPrefs `ftue_play_teach_done`). A legacy `grove.teach.order1.done` flag migrates to the new key.

Product owns the full lines. Design on-ring labels may be short.

| Beat | When | Highlight | Product copy | On-ring | Advance |
| --- | --- | --- | --- | --- | --- |
| T1 Crate | After Front Garden splash | Crate + `UI_Teach_Ring` | Tap the crate to grow supplies. | Tap to grow | Crate tap ≥1 |
| T2 Merge | After first spit / 2× matching | Two matching cells + `UI_Teach_Hand` loop | Drag two matches together. | Stack matches | One stack or merge; magnet/ghost if idle 4s |
| T3 Deliver | Order 1 WF T2 ready | Active order card + item | Deliver to Maya. | Deliver here | Complete Order 1 |

Skip (“Got it”) is offered **after T1** only if the player already acted correctly (already stacked/merged, or Order 1 already ready).

OUT: inventory / dig / herb lessons, FTUE 0–12.

DES-004 stubs: `UI_Teach_Ring`, `UI_Teach_Hand` (ring fallback: `ENV_FG_CellHighlight`).
