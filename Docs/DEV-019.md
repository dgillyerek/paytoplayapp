# DEV-019 — Play layout (board safe rect + HUD dock)

Status: **wired in Play Mode** (`Board.unity`) per `Docs/Design/play-hud-layout-v1.md`.

Portrait reference: **1080×1920**. Design Y% is from the **top**; Unity / `PlayLayout` use Y = 0 at the **bottom**.

## Acceptance

- Order tray rendered **only** inside the bottom dock (Design Y 70–100%). **Zero** overlap with cell rects.
- Board hero in Design Y **12–68%**. 7×5 is inset (`BoardSafeRect`) so the Garden Crate sits left/center on the board without covering cells.
- Top Design Y **0–10%**: energy left, Goal pill “Restore the Front Garden” center, coins right. Maya portrait small, top-right HUD band only.
- **≥16dp** gap between the 7×5 edge and any permanent HUD chrome (vertical or horizontal).
- Visual-only **5 inventory slots** in the dock (DEV-007 inventory is paused — no inventory sim).
- Visual parity with splash (Design art; stubs `UI_OrderDock_Panel`, `UI_GoalPill`).

## Bands

| Band | Design Y (from top) | Unity Y (bottom=0) | Contents |
| --- | --- | --- | --- |
| Top bar | 0–10% | 0.90–1.00 | Energy L, Goal pill C, coins R, Maya far R |
| Board | 12–68% | 0.32–0.88 | Backdrop + 7×5 inset + Garden Crate left. **No order cards.** |
| Bottom dock | 70–100% | 0.00–0.30 | `UI_OrderDock_Panel`, 3 order cards, 5 inventory slots, STARTER |

Math lives in `Grove.Domain.Layout.PlayLayout` (headless-tested). Unity HUD `Place()` and `GroveVisuals.FrameBoard` both read it.

DES-003 PNGs in `Assets/Grove/Art/Area1/UI/` (`UI_OrderDock_Panel`, `UI_GoalPill`). Optional `design/unity-drop` overlays the same stubs when present.
