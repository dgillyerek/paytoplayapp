# DEV-019 — Play layout (board safe rect + HUD dock)

Status: **wired in Play Mode** (`Board.unity`).

Portrait reference: **1080×1920**. Y = 0 at the bottom of the Game view.

## Acceptance

- Order tray / Maya / teach captions **never overlap playable cells**.
- Order tray docked **below** the board in a reserved HUD band (max **3** cards). Side-rail hook (`HudDockKind.SideRail`) waits on DES-003.
- **≥16dp** gap between the 7×5 edge (board safe rect) and any HUD chrome.
- Energy / coins stay **top**; they never collide with order cards.
- Maya portrait + **one-line** bubble live in the bottom HUD band only.
- Visual parity with splash (Design art; no debug chrome).

## Bands

| Band | Y (bottom–top) | Contents |
| --- | --- | --- |
| Top chrome | 0.87–0.98 | Goal, energy, coins, toast |
| **Board safe rect** | **0.26–0.86** | **Entire 7×5** (`PlayLayout.BoardSafeRect`, `BoardView` camera fit) |
| Bottom dock | 0.15–0.25 | Maya + one-line bubble + 3 order cards |
| Bottom producers | 0.01–0.14 | Crate + STARTER |

Math lives in `Grove.Domain.Layout.PlayLayout` (headless-tested). Unity HUD `Place()` and `GroveVisuals.FrameBoard` both read it.

DES-003 PNGs in `design/unity-drop` overlay `UI_OrderTray_Dock` / `UI_Maya_Bubble` when present.
