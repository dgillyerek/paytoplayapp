# DEV-018 — Front Garden splashes

Status: **wired in Play Mode** (`Board.unity`).

Sequence (tap to continue, full-screen Design PNGs):

1. **Boot** — `Splash_Boot_ProjectGrove` → “Project Grove”
2. **Area start** — `Splash_AreaStart_FrontGarden` → “Front Garden”
3. Play: goal **Restore the Front Garden**, Maya 2D portrait, orders 1–6 from `orders.json` (Order 1 = WF T2 ×1). After the area splash, DEV-020 T1–T3 (crate → two matches → deliver Order 1) — not full FTUE.
4. **Milestone** — completing Order 6 shows `Splash_Milestone_FrontGardenRestored` → “Front Garden Restored”

Copy comes from `copy.json` (do not fork). Domain hook: `SplashStub`. Art stubs live in `Assets/Grove/Art/Area1/manifest.json`.
