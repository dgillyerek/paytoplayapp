# DEV-002 — Item / recipe data (prototype)

Status: **in repo** (JSON + `CatalogLoader` + `MergeSession`).

Wildflower **T1–5** is the playable chain (T6–8 also listed). Herb T1 and Tools T1 are crate stubs. Recipes are **3→1** only.

Source of truth: `Assets/Grove/Resources/Grove/items.json` and `recipes.json`.

`MergeSession` never hardcodes chain length. QA can add a tier by editing JSON (and Play Mode will pick it up from Resources).
