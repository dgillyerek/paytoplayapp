# Bonequill AUTO_RIG_PATH

Meshy Animate walk **v2** (continuous AccuRIG body, props kept). Unity: `BonequillMeshyAnimateActor` plays `Walking` AS-IS on the native Humanoid. Path A cancelled. No Design PASS.
